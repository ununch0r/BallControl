namespace Core.Managers
{
    public interface ICompetitionManager
    {
        //method to merge competitions to the db, runs once a week


        //method to retrieve all competitions by countries for the specific date (includes count of fixtures)
        //could be better to select all fixtures by date and then filter by competitions
    }
}
